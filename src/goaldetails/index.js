import React from 'react';

// import useFetch from '../hooks/fetch';

export default function GoalDetails(props) {
    // const [data] = useFetch('https://developingyouapi.azurewebsites.net/api/goals');

    return (
        <ul>
            {/* {data.map((goal) => (
                <li>{goal.title} {goal.instances}</li>
            ))} */}
        </ul>
    )
}